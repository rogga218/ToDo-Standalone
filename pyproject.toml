[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
addopts = "-v --cov=src --cov-fail-under=68 --cov-report=term-missing"

[tool.coverage.run]
omit = ["src/main.py", "src/ui/routes.py"]
